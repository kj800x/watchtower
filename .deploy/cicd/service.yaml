apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: watchtower
  name: watchtower
  namespace: cicd
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 8080
  selector:
    app: watchtower
  type: LoadBalancer
